import 'package:flutter/material.dart';

class AppColors {
  static const primaryBg = const Color.fromARGB(255, 0, 15, 100);
  static const primaryText = Colors.white;
  static const primaryBox = Color.fromARGB(255, 230, 230, 230);
}
